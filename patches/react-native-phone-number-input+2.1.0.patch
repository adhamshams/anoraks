diff --git a/node_modules/react-native-phone-number-input/lib/index.js b/node_modules/react-native-phone-number-input/lib/index.js
index b89c388..ddacbd7 100644
--- a/node_modules/react-native-phone-number-input/lib/index.js
+++ b/node_modules/react-native-phone-number-input/lib/index.js
@@ -8,6 +8,7 @@ import CountryPicker, {
   Flag,
 } from "react-native-country-picker-modal";
 import { PhoneNumberUtil } from "google-libphonenumber";
+import { BottomSheetTextInput } from '@gorhom/bottom-sheet';
 import styles from "./styles";
 
 const dropDown =
@@ -25,6 +26,7 @@ export default class PhoneInput extends PureComponent {
         ? props.defaultValue
         : "",
       modalVisible: false,
+      focus: props.focus,
       countryCode: props.defaultCode ? props.defaultCode : "IN",
       disabled: props.disabled || false,
     };
@@ -159,8 +161,10 @@ export default class PhoneInput extends PureComponent {
       filterProps = {},
       countryPickerButtonStyle,
       layout = "first",
+      focus
     } = this.props;
     const { modalVisible, code, countryCode, number, disabled } = this.state;
+    focus ? this.textInput.focus() : null;
     return (
       <CountryModalProvider>
         <View
@@ -219,16 +223,20 @@ export default class PhoneInput extends PureComponent {
                 style={[styles.codeText, codeTextStyle ? codeTextStyle : {}]}
               >{`+${code}`}</Text>
             )}
-            <TextInput
+            <BottomSheetTextInput
               style={[styles.numberText, textInputStyle ? textInputStyle : {}]}
-              placeholder={placeholder ? placeholder : "Phone Number"}
+              placeholder={placeholder ? placeholder : "Ex: 1156233765"}
               onChangeText={this.onChangeText}
+              ref={(input) => {this.textInput = input}}
               value={number}
+              autoComplete="tel-country-code"
+              textContentType="telephoneNumber"
               editable={disabled ? false : true}
-              selectionColor="black"
               keyboardAppearance={withDarkTheme ? "dark" : "default"}
               keyboardType="number-pad"
               autoFocus={autoFocus}
+              placeholderTextColor={'rgba(0,0,0,0.3)'}
               {...textInputProps}
             />
           </View>
diff --git a/node_modules/react-native-phone-number-input/lib/styles.js b/node_modules/react-native-phone-number-input/lib/styles.js
index 1789ce7..2d728a6 100644
--- a/node_modules/react-native-phone-number-input/lib/styles.js
+++ b/node_modules/react-native-phone-number-input/lib/styles.js
@@ -1,4 +1,5 @@
 import {StyleSheet, Dimensions} from 'react-native';
+import {RFValue} from 'react-native-responsive-fontsize';
 const {width: viewportWidth, height: viewportHeight} = Dimensions.get('window');
 function wp(percentage) {
   const value = (percentage * viewportWidth) / 100;
@@ -38,18 +39,19 @@ const styles = StyleSheet.create({
   dropDownImage: {
     height: 14,
     width: 12,
+    tintColor: '#124c7d'
   },
   textContainer: {
     flex: 1,
     backgroundColor: '#F8F9F9',
-    paddingHorizontal: wp(4),
+    paddingHorizontal: wp(0),
     paddingVertical: hp(2),
     textAlign: 'left',
     flexDirection: 'row',
     alignItems: 'center',
   },
   codeText: {
-    fontSize: 16,
+    fontSize: RFValue(15),
     marginRight: 10,
     fontWeight: '500',
     color: '#000000',
